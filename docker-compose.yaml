version: '3.8'

services:
  forum-web:
    build:
      context: .
      args:
        - VITE_APP_API_URL=http://localhost:8080/api/v1
    container_name: forum-react-web
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - forum-network

networks:
  forum-network:
    driver: bridge